Python (de Microsoft)
Pylance (de Microsoft)
Better Jinja (de samuelcolvin)
DotENV (de mikestead)
SQLite Viewer (de Florian Klampfer):

terminal ejecutar

New-Item -ItemType Directory templates, static
New-Item -ItemType File app.py, database.py, routes.py, requirements.txt, .env

# Crear entorno virtual
python -m venv .venv
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
# Activar en Windows (PowerShell)
.venv\Scripts\Activate.ps1
# O en Git Bash / Linux / macOS:
# source .venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
pip freeze > requirements.txt  